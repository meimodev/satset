# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.

include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.drift.dart"
    - "lib/server/db/**/*.g.dart"
    # gen-l10n output (ADR-0083). Regenerated by `flutter gen-l10n`; edit the
    # ARB pair, never these.
    - "lib/l10n/app_localizations*.dart"
    # drift schema snapshots (`drift_dev schema generate`). Regenerated
    # whenever schemaVersion is bumped; see test/schema_migration_test.dart.
    - "test/generated/**"
    - "build/**"

linter:
  rules:
    # avoid_print: false
    # prefer_single_quotes: true
